<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Course Catalog{% endblock %}</title>
        <link rel="icon" href="{{ asset('favicon.ico') }}">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        <script type="text/javascript">
            // Allow templates to register asset paths globally.
            let assets = {
                {% block assets %}{% endblock %}
            };
        </script>

            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
